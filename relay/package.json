{
  "name": "wechat-relay",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Stateless relay that forwards only /cgi-bin/* requests to api.weixin.qq.com so WeChat sees a fixed egress IP.",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node dist/server.js"
  },
  "engines": {
    "node": ">=22"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.6.0"
  }
}
