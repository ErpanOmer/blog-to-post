请分析以下文章内容，并返回一个 JSON 对象。

### 文章内容：
---
{{ARTICLE_CONTENT}}
---

### 输出要求：
请严格按照以下 JSON 结构返回（不要包含 Markdown 代码块标记，直接返回 JSON 字符串）：

{
  "summary": "一段话概况这篇文章到底了说了什么，作为文章摘要说明，不要超过80个汉字",
  "tags": ["涉及的核心技术标签,（可以是中文，但不要超过4个汉字）"]
}